# Client Environment Configuration
# Copy to `.env.local` and fill in values.

# Privy client App ID (Required)
# Get from https://dashboard.privy.io
NEXT_PUBLIC_PRIVY_APP_ID=your_privy_app_id

# Backend API URL (Required for local dev)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Counter demo (read + write)
# The backend can also read this as a fallback if its own COUNTER_CONTRACT_ADDRESS is not set
NEXT_PUBLIC_CONTRACT_ADDRESS=

# (Optional) Explorer base for tx links (defaults to Voyager Sepolia)
# NEXT_PUBLIC_EXPLORER_TX_BASE=https://sepolia.voyager.online/tx
